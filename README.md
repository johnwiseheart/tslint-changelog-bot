tsling-changelog-bot
=========================

This is a simple bot which monitors PRs, and adds a failed check if there is no changelog entry in the body of the PR.

---

## Getting Started

To run, use `yarn start`.